import styled from "styled-components";

export const Container = styled.div`
  padding: 1vh 2rem;
`;

export const BigHeadline = styled.h1`
  color: var(--clr-blue);
  margin-top: 30px;
  font-size: 3rem;
  font-weight: 800;
  text-align: center;
`;

export const Headline = styled.h2`
  text-decoration: underline;
  color: var(--clr-blue);
  margin-top: 15px;
  font-size: 3rem;
  font-weight: 800;
`;

export const DevName = styled.h4`
  color: var(--clr-blue);
  margin-top: 15px;
  font-size: 2.2rem;
  font-weight: 800;
`;

export const DevDesc = styled.h6`
  color: var(--clr-text-grey);
  margin-top: 5px;
  font-size: 1.7rem;
  font-weight: 800;
`;
